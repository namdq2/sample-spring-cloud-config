server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/namdq2/sample-config-cloud
    bus:
      enabled: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
